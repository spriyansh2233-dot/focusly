{
  "name": "focusly-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm --prefix frontend run start",
    "backend": "cd backend && ./mvnw spring-boot:run"
  }
}
